{
  "name": "klaviyo-hotel-tracking",
  "version": "1.0.0",
  "description": "Klaviyo hotel tracking for GTM",
  "scripts": {
    "build": "esbuild src/klaviyo_hotel_tracking.js --bundle --outfile=klaviyo_hotel_tracking.js --format=iife",
    "deploy": "npm run build && surge",
    "watch": "esbuild src/klaviyo_hotel_tracking.js --bundle --outfile=klaviyo_hotel_tracking.js --format=iife --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
